import React from 'react';
import {withRouter} from 'react-router-dom';

const Profile = (props)=>{
    console.log(props)
return(
    <React.Fragment>
        <h2>Profile id : </h2>
    </React.Fragment>
)
}

export default withRouter(Profile);